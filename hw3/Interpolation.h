#include "std_lib_facilities.h"
#include <map>
class Interpolation{
public:
    map<double, double> input;
    vector<vector<int>> poly;
    double xx;
    Interpolation(double x) : xx(x){
        
    }

    
}